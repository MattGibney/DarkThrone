# Website Notes

- Keep the Vite `allowedHosts` list aligned with the local worktree domains from the workspace notes, especially `.darkthrone.test` and `.darkthrone.local`, so `tools/dev` environments can load the marketing site without host validation failures.
